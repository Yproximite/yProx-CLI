query LoadMediaCategories($siteId: Int!) {
  site(siteId: $siteId) {
    mediaCategories {
      id
      title
      hidden
      repository
      createdAt
      updatedAt
      site {
        id
      }
    }
  }
}

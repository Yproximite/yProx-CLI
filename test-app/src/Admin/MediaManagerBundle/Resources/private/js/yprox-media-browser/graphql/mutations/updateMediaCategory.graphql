#import "../fragments/mediaCategory.graphql"

mutation UpdateMediaCategory(
  $siteId: Int!
  $mediaCategoryId: Int!
  $input: UpdateMediaCategoryInput!
) {
  updateMediaCategory(
    siteId: $siteId
    mediaCategoryId: $mediaCategoryId
    input: $input
  ) {
      ...MediaCategoryFragment
  }
}
